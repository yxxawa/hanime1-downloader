<Window x:Class="Hanime1Downloader.CSharp.Views.CloudflareWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:wv2="clr-namespace:Microsoft.Web.WebView2.Wpf;assembly=Microsoft.Web.WebView2.Wpf"
        Title="Cloudflare 验证"
        Height="480"
        Width="520"
        MinHeight="380"
        MinWidth="420"
        WindowStartupLocation="CenterOwner"
        Background="#F6F6F6">
    <Window.Resources>
        <Style TargetType="Button">
            <Setter Property="Padding" Value="8,0"/>
            <Setter Property="Height" Value="28"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Background" Value="#FFFFFF"/>
            <Setter Property="BorderBrush" Value="#C8D0DA"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="2">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="PrimaryButtonStyle" TargetType="Button" BasedOn="{StaticResource {x:Type Button}}">
            <Setter Property="Background" Value="#1677FF"/>
            <Setter Property="BorderBrush" Value="#1677FF"/>
            <Setter Property="Foreground" Value="White"/>
        </Style>
    </Window.Resources>
    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Border Padding="8"
                Background="#FCFCFD"
                BorderBrush="#D9E0E7"
                BorderThickness="1"
                CornerRadius="2">
            <DockPanel LastChildFill="False">
                <Button x:Name="FinishButton"
                        DockPanel.Dock="Right"
                        Width="124"
                        Margin="12,0,0,0"
                        IsEnabled="False"
                        Click="FinishButton_OnClick"
                        Content="验证完成并继续"
                        Style="{StaticResource PrimaryButtonStyle}"/>
                <TextBlock x:Name="StatusText"
                           VerticalAlignment="Center"
                           Foreground="#374151"
                           FontSize="11"
                           TextWrapping="Wrap"
                           Text="请在内置浏览器中完成 Cloudflare 验证。"/>
            </DockPanel>
        </Border>

        <Border Grid.Row="1"
                Margin="0,8,0,0"
                Background="#FFFFFF"
                BorderBrush="#D9E0E7"
                BorderThickness="1"
                CornerRadius="2">
            <wv2:WebView2 x:Name="Browser" Margin="0" />
        </Border>
    </Grid>
</Window>
